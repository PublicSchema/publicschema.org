# ILO SDMX SDG metric catalog seed.
#
# Generated by build/seed_metrics_from_ilo_sdmx.py.
# Source: ILO SDMX 2.1 registry (https://sdmx.ilo.org/rest/)
# Filter: allowlist of SDG dataflows derivable (now or with a small
# PublicSchema slot addition) from record-level PS data.
# Excludes macro indicators (8.2.1 GDP/worker, 8.5.1 wages, 10.4.1 income share),
# country-level qualitative (8.8.2, 8.b.1), and injury registry (8.8.1).
# Retrieved at: 2026-05-14T09:38:34Z
# Indicator count: 17
# License: Reproduction of ILO material is permitted for non-commercial purposes per https://www.ilo.org/rights-and-permissions
#
# Calculation blocks are out of scope for this converter;
# docs/metrics-spec.md line 310 prescribes hand-authoring for the derivable subset.

id: ilo_sdg_seed
title: ILO SDMX SDG indicators relevant to social protection — seed metric catalog
description: |
  Auto-generated seed from ILO's SDMX 2.1 dataflow registry, filtered to SDG
  indicators relevant to social protection delivery. The selection rule is
  derivability from PublicSchema record-level data: each kept indicator can be
  computed (now or with a small slot addition) from Person.employment_status,
  Person.occupation, Person.industry, Person.status_in_employment,
  Enrollment in Program, etc. Macro indicators and country-level qualitative
  ratings are excluded. Trilingual labels (en/es/fr) come from the SDMX
  <common:Name> elements; dimensions and attributes are derived from each
  DSD's authoritative DimensionList and AttributeList. Calculations, concept
  URIs, and dimension/attribute catalogue definitions are out of scope for
  this seed and tracked as TODOs.
source: ILO SDMX (https://sdmx.ilo.org/rest/)
license: "Reproduction of ILO material is permitted for non-commercial purposes per https://www.ilo.org/rights-and-permissions"

metrics:

  # ILO dataflow: DF_SDG_0111_SEX_AGE_RT (v1.0)
  # DSD: SDG_0111_SEX_AGE_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, SEX, AGE, TIME_PERIOD
  sdg_0111_sex_age_rt:
    title: "SDG indicator 1.1.1 - Working poverty rate by sex and age (percentage of employed living below US$3 PPP)"
    annotations:
      label_es: "Indicador ODS 1.1.1 - Tasa de trabajadores pobres según sexo y edad (porcentaje de ocupados que viven con menos de US$3 PPA)"
      label_fr: "Indicateur ODD 1.1.1 - Taux de travailleurs pauvres par sexe et âge (pourcentage de personnes en emploi vivant avec moins de US$3 PPA)"
      description_es: "Indicador ODS 1.1.1 - Tasa de trabajadores pobres según sexo y edad (porcentaje de ocupados que viven con menos de US$3 PPA)"
      description_fr: "Indicateur ODD 1.1.1 - Taux de travailleurs pauvres par sexe et âge (pourcentage de personnes en emploi vivant avec moins de US$3 PPA)"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_0111 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/sex
      - publicschema:dim/age
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_0111_SEX_AGE_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_0131_SEX_SOC_RT (v1.0)
  # DSD: SDG_0131_SEX_SOC_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, SEX, SOC, TIME_PERIOD
  sdg_0131_sex_soc_rt:
    title: "SDG indicator 1.3.1 - Proportion of population covered by social protection floors/systems by sex and function"
    annotations:
      label_es: "Indicador ODS 1.3.1 - Proporción de la población cubierta por sistemas/pisos de protección social según sexo y función"
      label_fr: "Indicateur ODD 1.3.1 - Proportion de la population couverte par des systèmes/socles de protection sociale par sexe et fonction"
      description_es: "Indicador ODS 1.3.1 - Proporción de la población cubierta por sistemas/pisos de protección social según sexo y función"
      description_fr: "Indicateur ODD 1.3.1 - Proportion de la population couverte par des systèmes/socles de protection sociale par sexe et fonction"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    topic: coverage
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/sex
      - publicschema:dim/soc
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_0131_SEX_SOC_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_0552_NOC_RT (v1.0)
  # DSD: SDG_0552_NOC_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, TIME_PERIOD
  sdg_0552_noc_rt:
    title: "SDG indicator 5.5.2 - Proportion of women in senior and middle management positions"
    annotations:
      label_es: "Indicador ODS 5.5.2 - Proporción de mujeres en puestos gerenciales o directivos superiores y medios"
      label_fr: "Indicateur ODD 5.5.2 - Proportion des femmes occupant des postes de direction supérieurs et intermédiaires"
      description_es: "Indicador ODS 5.5.2 - Proporción de mujeres en puestos gerenciales o directivos superiores y medios"
      description_fr: "Indicateur ODD 5.5.2 - Proportion des femmes occupant des postes de direction supérieurs et intermédiaires"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_0552 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_0552_NOC_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_0831_SEX_ECO_RT (v1.0)
  # DSD: SDG_0831_SEX_ECO_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, SEX, ECO, TIME_PERIOD
  sdg_0831_sex_eco_rt:
    title: "SDG indicator 8.3.1 - Proportion of informal employment in total employment by sex and economic activity"
    annotations:
      label_es: "Indicador ODS 8.3. 1 - Proporción de la ocupación informal en la ocupación total según sexo y actividad económica"
      label_fr: "Indicateur ODD 8.3.1 - Proportion de l'emploi informel dans l'emploi total par sexe et activité économique"
      description_es: "Indicador ODS 8.3. 1 - Proporción de la ocupación informal en la ocupación total según sexo y actividad económica"
      description_fr: "Indicateur ODD 8.3.1 - Proportion de l'emploi informel dans l'emploi total par sexe et activité économique"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_0831 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/sex
      - publicschema:dim/eco
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_0831_SEX_ECO_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_0852_SEX_AGE_RT (v1.0)
  # DSD: SDG_0852_SEX_AGE_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, SEX, AGE, TIME_PERIOD
  sdg_0852_sex_age_rt:
    title: "SDG indicator 8.5.2 - Unemployment rate by sex and age"
    annotations:
      label_es: "Indicador ODS 8.5.2 - Tasa de desocupación según sexo y edad"
      label_fr: "Indicateur ODD 8.5.2 - Taux de chômage par sexe et âge"
      description_es: "Indicador ODS 8.5.2 - Tasa de desocupación según sexo y edad"
      description_fr: "Indicateur ODD 8.5.2 - Taux de chômage par sexe et âge"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_0852 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/sex
      - publicschema:dim/age
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_0852_SEX_AGE_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_0852_SEX_DSB_RT (v1.0)
  # DSD: SDG_0852_SEX_DSB_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, SEX, DSB, TIME_PERIOD
  sdg_0852_sex_dsb_rt:
    title: "SDG indicator 8.5.2 - Unemployment rate by sex and disability status"
    annotations:
      label_es: "Indicador ODS 8.5.2 - Tasa de desocupación según sexo y situación en términos de discapacidad"
      label_fr: "Indicateur ODD 8.5.2 - Taux de chômage par sexe et situation en termes d'handicap"
      description_es: "Indicador ODS 8.5.2 - Tasa de desocupación según sexo y situación en términos de discapacidad"
      description_fr: "Indicateur ODD 8.5.2 - Taux de chômage par sexe et situation en termes d'handicap"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_0852 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/sex
      - publicschema:dim/dsb
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_0852_SEX_DSB_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_0861_SEX_RT (v1.0)
  # DSD: SDG_0861_SEX_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, SEX, TIME_PERIOD
  sdg_0861_sex_rt:
    title: "SDG indicator 8.6.1 - Proportion of youth (aged 15-24 years) not in education, employment or training by sex"
    annotations:
      label_es: "Indicador ODS 8.6.1 - Proporción de jóvenes (de 15 a 24 años) que no están en el sistema educativo ni ocupados ni en formación según sexo"
      label_fr: "Indicateur ODD 8.6.1 - Proportion de jeunes (âgés de 15 à 24 ans) ni dans le système éducatif, ni en emploi ni en formation par sexe"
      description_es: "Indicador ODS 8.6.1 - Proporción de jóvenes (de 15 a 24 años) que no están en el sistema educativo ni ocupados ni en formación según sexo"
      description_fr: "Indicateur ODD 8.6.1 - Proportion de jeunes (âgés de 15 à 24 ans) ni dans le système éducatif, ni en emploi ni en formation par sexe"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_0861 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/sex
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_0861_SEX_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_0922_NOC_RT (v1.0)
  # DSD: SDG_0922_NOC_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, TIME_PERIOD
  sdg_0922_noc_rt:
    title: "SDG indicator 9.2.2 - Manufacturing employment as a proportion of total employment"
    annotations:
      label_es: "Indicador ODS 9.2.2 - Ocupación en la manufactura como porcentaje de la ocupación total"
      label_fr: "Indicateur ODD 8.8.2 - Emploi dans l'industrie manufacturière en pourcentage de l'emploi total"
      description_es: "Indicador ODS 9.2.2 - Ocupación en la manufactura como porcentaje de la ocupación total"
      description_fr: "Indicateur ODD 8.8.2 - Emploi dans l'industrie manufacturière en pourcentage de l'emploi total"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_0922 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_0922_NOC_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_A871_SEX_AGE_RT (v1.0)
  # DSD: SDG_A871_SEX_AGE_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, SEX, AGE, TIME_PERIOD
  sdg_a871_sex_age_rt:
    title: "SDG indicator 8.7.1 - Proportion of children engaged in economic activity by sex and age"
    annotations:
      label_es: "Indicador ODS 8.7.1 - Porcentaje de niños ocupados en actividades económicas según sexo y edad"
      label_fr: "Indicateur ODD 8.7.1 - Proportion d'enfants exerçant une activité économique par sexe et âge"
      description_es: "Indicador ODS 8.7.1 - Porcentaje de niños ocupados en actividades económicas según sexo y edad"
      description_fr: "Indicateur ODD 8.7.1 - Proportion d'enfants exerçant une activité économique par sexe et âge"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_A871 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/sex
      - publicschema:dim/age
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_A871_SEX_AGE_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_B552_NOC_RT (v1.0)
  # DSD: SDG_B552_NOC_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, TIME_PERIOD
  sdg_b552_noc_rt:
    title: "SDG indicator 5.5.2 - Proportion of women in senior and middle management positions -- 19th ICLS"
    annotations:
      label_es: "Indicador ODS 5.5.2 - Proporción de mujeres en puestos gerenciales o directivos superiores y medios -- 19 ICLS"
      label_fr: "Indicateur ODD 5.5.2 - Proportion des femmes occupant des postes de direction supérieurs et intermédiaires -- 19e ICLS"
      description_es: "Indicador ODS 5.5.2 - Proporción de mujeres en puestos gerenciales o directivos superiores y medios -- 19 ICLS"
      description_fr: "Indicateur ODD 5.5.2 - Proportion des femmes occupant des postes de direction supérieurs et intermédiaires -- 19e ICLS"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_B552 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_B552_NOC_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_B831_SEX_ECO_RT (v1.0)
  # DSD: SDG_B831_SEX_ECO_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, SEX, ECO, TIME_PERIOD
  sdg_b831_sex_eco_rt:
    title: "SDG indicator 8.3.1 - Proportion of informal employment in total employment by sex and economic activity -- 19th ICLS"
    annotations:
      label_es: "Indicador ODS 8.3. 1 - Proporción de la ocupación informal en la ocupación total según sexo y actividad económica -- 19 ICLS"
      label_fr: "Indicateur ODD 8.3.1 - Proportion de l'emploi informel dans l'emploi total par sexe et activité économique -- 19e ICLS"
      description_es: "Indicador ODS 8.3. 1 - Proporción de la ocupación informal en la ocupación total según sexo y actividad económica -- 19 ICLS"
      description_fr: "Indicateur ODD 8.3.1 - Proportion de l'emploi informel dans l'emploi total par sexe et activité économique -- 19e ICLS"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_B831 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/sex
      - publicschema:dim/eco
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_B831_SEX_ECO_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_B852_SEX_AGE_RT (v1.0)
  # DSD: SDG_B852_SEX_AGE_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, SEX, AGE, TIME_PERIOD
  sdg_b852_sex_age_rt:
    title: "SDG indicator 8.5.2 - Unemployment rate by sex and age -- 19th ICLS"
    annotations:
      label_es: "Indicador ODS 8.5.2 - Tasa de desocupación según sexo y edad -- 19 ICLS"
      label_fr: "Indicateur ODD 8.5.2 - Taux de chômage par sexe et âge -- 19e ICLS"
      description_es: "Indicador ODS 8.5.2 - Tasa de desocupación según sexo y edad -- 19 ICLS"
      description_fr: "Indicateur ODD 8.5.2 - Taux de chômage par sexe et âge -- 19e ICLS"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_B852 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/sex
      - publicschema:dim/age
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_B852_SEX_AGE_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_B852_SEX_DSB_RT (v1.0)
  # DSD: SDG_B852_SEX_DSB_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, SEX, DSB, TIME_PERIOD
  sdg_b852_sex_dsb_rt:
    title: "SDG indicator 8.5.2 - Unemployment rate by sex and disability status -- 19th ICLS"
    annotations:
      label_es: "Indicador ODS 8.5.2 - Tasa de desocupación según sexo y situación en términos de discapacidad -- 19 ICLS"
      label_fr: "Indicateur ODD 8.5.2 - Taux de chômage par sexe et situation en termes d'handicap -- 19e ICLS"
      description_es: "Indicador ODS 8.5.2 - Tasa de desocupación según sexo y situación en términos de discapacidad -- 19 ICLS"
      description_fr: "Indicateur ODD 8.5.2 - Taux de chômage par sexe et situation en termes d'handicap -- 19e ICLS"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_B852 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/sex
      - publicschema:dim/dsb
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_B852_SEX_DSB_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_B871_SEX_AGE_RT (v1.0)
  # DSD: SDG_B871_SEX_AGE_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, SEX, AGE, TIME_PERIOD
  sdg_b871_sex_age_rt:
    title: "SDG indicator 8.7.1 - Proportion of children engaged in economic activity and household chores by sex and age"
    annotations:
      label_es: "Indicador ODS 8.7.1 - Proporción de niños ocupados en actividades económicas y tareas domésticas según sexo y edad"
      label_fr: "Indicateur ODD 8.7.1 - Proportion d'enfants engagés dans des activités économiques et des tâches ménagères par sexe et âge"
      description_es: "Indicador ODS 8.7.1 - Proporción de niños ocupados en actividades económicas y tareas domésticas según sexo y edad"
      description_fr: "Indicateur ODD 8.7.1 - Proportion d'enfants engagés dans des activités économiques et des tâches ménagères par sexe et âge"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_B871 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/sex
      - publicschema:dim/age
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_B871_SEX_AGE_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_B922_NOC_RT (v1.0)
  # DSD: SDG_B922_NOC_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, TIME_PERIOD
  sdg_b922_noc_rt:
    title: "SDG indicator 9.2.2 - Manufacturing employment as a proportion of total employment -- 19th ICLS"
    annotations:
      label_es: "Indicador ODS 9.2.2 - Ocupación en la manufactura como porcentaje de la ocupación total -- 19 CIET"
      label_fr: "Indicateur ODD 8.8.2 - Emploi dans l'industrie manufacturière en pourcentage de l'emploi total -- 19e ICLS"
      description_es: "Indicador ODS 9.2.2 - Ocupación en la manufactura como porcentaje de la ocupación total -- 19 CIET"
      description_fr: "Indicateur ODD 8.8.2 - Emploi dans l'industrie manufacturière en pourcentage de l'emploi total -- 19e ICLS"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_B922 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_B922_NOC_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_T552_NOC_RT (v1.0)
  # DSD: SDG_T552_NOC_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, TIME_PERIOD
  sdg_t552_noc_rt:
    title: "SDG indicator 5.5.2 - Proportion of women in managerial positions"
    annotations:
      label_es: "Indicador ODS 5.5.2 - Proporción de mujeres en puestos gerenciales o directivos"
      label_fr: "Indicateur ODD 5.5.2 - Proportion des femmes occupant des postes de direction"
      description_es: "Indicador ODS 5.5.2 - Proporción de mujeres en puestos gerenciales o directivos"
      description_fr: "Indicateur ODD 5.5.2 - Proportion des femmes occupant des postes de direction"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_T552 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_T552_NOC_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

  # ILO dataflow: DF_SDG_U552_NOC_RT (v1.0)
  # DSD: SDG_U552_NOC_RT (v1.0)
  # Dimensions (DSD order): REF_AREA, FREQ, MEASURE, TIME_PERIOD
  sdg_u552_noc_rt:
    title: "SDG indicator 5.5.2 - Proportion of women in managerial positions -- 19th ICLS"
    annotations:
      label_es: "Indicador ODS 5.5.2 - Proporción de mujeres en puestos gerenciales o directivos -- 19 ICLS"
      label_fr: "Indicateur ODD 5.5.2 - Proportion des femmes occupant des postes de direction -- 19e ICLS"
      description_es: "Indicador ODS 5.5.2 - Proporción de mujeres en puestos gerenciales o directivos -- 19 ICLS"
      description_fr: "Indicateur ODD 5.5.2 - Proportion des femmes occupant des postes de direction -- 19e ICLS"
    value_type: proportion
    # TODO: derive unit — title does not contain (%); confirm UNIT_MEASURE from ILOSTAT
    decimals: 1
    # TODO: map SDG indicator prefix SDG_U552 to MetricTopic
    dimensions:
      - publicschema:dim/ref_area
      - publicschema:dim/freq
      - publicschema:dim/time_period
    attributes:
      - publicschema:attr/obs_status
      - publicschema:attr/unit_measure_type
      - publicschema:attr/unit_measure
      - publicschema:attr/unit_mult
      # TODO: non-standard attribute SOURCE — confirm whether to include
      - publicschema:attr/source
      # TODO: non-standard attribute NOTE_SOURCE — confirm whether to include
      - publicschema:attr/note_source
      # TODO: non-standard attribute NOTE_INDICATOR — confirm whether to include
      - publicschema:attr/note_indicator
      # TODO: non-standard attribute NOTE_CLASSIF — confirm whether to include
      - publicschema:attr/note_classif
      # TODO: non-standard attribute DECIMALS — confirm whether to include
      - publicschema:attr/decimals
      # TODO: non-standard attribute UPPER_BOUND — confirm whether to include
      - publicschema:attr/upper_bound
      # TODO: non-standard attribute LOWER_BOUND — confirm whether to include
      - publicschema:attr/lower_bound
    aligns_with:
      - "urn:sdmx:org.sdmx.infomodel.datastructure.Dataflow=ILO:DF_SDG_U552_NOC_RT(1.0)"
      # TODO: UN SDG series URI
    core: true
    version: "1.0.0"
    status: bibo:draft

dimensions_referenced:
  publicschema:dim/age: "ILO concept AGE (ConceptScheme CS_CLASSIF_TYPE, Codelist CL_AGE)"
  publicschema:dim/dsb: "ILO concept DSB (ConceptScheme CS_CLASSIF_TYPE, Codelist CL_DSB)"
  publicschema:dim/eco: "ILO concept ECO (ConceptScheme CS_CLASSIF_TYPE, Codelist CL_ECO)"
  publicschema:dim/freq: "ILO concept FREQ (ConceptScheme CS_ILOSTAT, Codelist CL_FREQ)"
  publicschema:dim/ref_area: "ILO concept REF_AREA (ConceptScheme CS_ILOSTAT, Codelist CL_AREA)"
  publicschema:dim/sex: "ILO concept SEX (ConceptScheme CS_CLASSIF_TYPE, Codelist CL_SEX)"
  publicschema:dim/soc: "ILO concept SOC (ConceptScheme CS_CLASSIF_TYPE, Codelist CL_SOC)"
  publicschema:dim/time_period: "SDMX TimeDimension TIME_PERIOD"

attributes_referenced:
  publicschema:attr/decimals: "ILO concept DECIMALS"
  publicschema:attr/lower_bound: "ILO concept LOWER_BOUND"
  publicschema:attr/note_classif: "ILO concept NOTE_CLASSIF"
  publicschema:attr/note_indicator: "ILO concept NOTE_INDICATOR"
  publicschema:attr/note_source: "ILO concept NOTE_SOURCE"
  publicschema:attr/obs_status: "ILO concept OBS_STATUS"
  publicschema:attr/source: "ILO concept SOURCE"
  publicschema:attr/unit_measure: "ILO concept UNIT_MEASURE"
  publicschema:attr/unit_measure_type: "ILO concept UNIT_MEASURE_TYPE"
  publicschema:attr/unit_mult: "ILO concept UNIT_MULT"
  publicschema:attr/upper_bound: "ILO concept UPPER_BOUND"
